spring:
  application:
    name: Editorial BFF
server:
  port: 8080

# --- DeepL ---
deepl:
  auth-key: ${DEEPL_AUTH_KEY:}

# --- CORS (for local frontend dev) ---
cors:
  allowed-origins: "http://localhost:5173"


