# Development profile: enables the OpenAPI docs at http://localhost:8091/swagger-ui.html.
# Not for a deployed environment. Same pattern as the gateway's local profile.
springdoc:
  api-docs:
    enabled: true
  swagger-ui:
    enabled: true
